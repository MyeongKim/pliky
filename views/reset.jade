//
   Created by nuko on 2015. 6. 26..
extends layout
block content
    script.
        $(document).ready(function(){
            $("form").submit(function (e) {
                if ($('input[name="email"]').val() == '' || $('input[name="password"]').val() == ''){
                    e.preventDefault();
                }
            });
        });
    .container
        .row
            .col.s12.m8.offset-m2.l6.offset-l3.z-depth-1
                .row.forgotDiv
                    .col.s12.forgotInput
                        p.center-align 비밀번호 재설정
                        form(method="post")
                            .row
                                .input-field.col.s12
                                    input#email.validate(type='password' name='password' autofocus)
                                    label(for='password') 새 비밀번호
                            .row
                                .input-field.col.s12
                                    input#email.validate(type='password' name='confirm' autofocus)
                                    label(for='confirm') 새 비밀번호 확인
                            button.btn.waves-effect.waves-light.right.black.signButton(type='submit', name='action') 재설정
//
   Created by nuko on 2015. 6. 29..

