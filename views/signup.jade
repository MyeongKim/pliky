//
   Created by nuko on 2015. 6. 26..

extends layout

block content
    script.
        $(document).ready(function(){
            $("form").submit(function (e) {
                if ($('input[name="password"]').val() != $('input[name="passwordAgain"]').val()) {
                    e.preventDefault();
                    alert("비밀번호가 같지 않습니다.");
                    $('input[name="password"]').val('');
                    $('input[name="passwordAgain"]').val('');
                } else if ($('input[name="password"]').val() == '' || $('input[name="passwordAgain"]').val() == ''){
                    e.preventDefault();
                }
            });
        });

    .container
        .row
            .col.s12.m8.offset-m2.l6.offset-l3.z-depth-1
                .row.signDiv
                    .col.s4.logos.black
                        p.white-text 5초만에 가입하기
                        a.social-button(href="#",class='facebook')
                            i.fa(class="fa-"+'facebook')
                        a.social-button(href="#",class='twitter')
                            i.fa(class="fa-"+'twitter')
                        a.social-button(href="#",class='google')
                            i.fa(class="fa-"+'google')
                    .col.s8.signInput
                        form(action="/signup", method="post")
                            .row
                                .input-field.col.s12
                                    input#email.validate(type='email' name="email" autofocus)
                                    label(for='email') Email
                            .row
                                .input-field.col.s12
                                    input#password.validate(type='password' name="password")
                                    label(for='password') Password
                            .row
                                .input-field.col.s12
                                    input#passwordAgain.validate(type='password' name="passwordAgain")
                                    label(for='passwordAgain') Password(again)
                            button.btn.waves-effect.waves-light.right.black.signButton(type='submit', name='action') 가입하기
                    .signUpQuestion.right
                        p 이미 계정이 있으신가요?
                            a(href='/login') 로그인